# Codex

STATUS: done
TASK: Establish a reproducible local Demo Analyzer development baseline
BRANCH: `dev/v1-foundation`
CHANGED: Added `tools/dev/Setup-V1.ps1` as an idempotent Python 3.13 bootstrap for the existing Demo Analyzer. It creates `.venv` only when missing, refuses an existing environment with the wrong Python minor version instead of deleting it, installs exact dependencies from `requirements.lock`, installs the project editable with `--no-deps`, runs `pip check`, runs the test suite unless explicitly skipped, verifies the `iy-analyze` entry point, and smoke-tests `--help`; every native step is exit-code checked. Updated the root and tools READMEs, changelog, and cockpit without taking ownership of the separate free 2D Viewer preparation workstream. No Analyzer behavior, schema, parsing rule, or dependency version changed.
VERIFIED: PowerShell AST parsing reports no syntax errors. A fresh Python 3.13 `.venv` was created and populated successfully from all 35 locked packages; editable package build/install succeeded. `pip check` reports no broken requirements. `pytest` passes `7/7` in both the direct baseline check and the rerun through `Setup-V1.ps1`. Package imports succeed. `iy-analyze --help` succeeds. Missing-demo and unsupported-type CLI paths both return exit code `2` without writing analysis output. A second setup run safely reused the existing `.venv` and completed `PASS: V1 development baseline is ready.`
DECISIONS: Python 3.13 plus `requirements.lock` is the reproducible development baseline. Setup may create a missing ignored `.venv` but must never silently delete or replace an existing environment. Locked dependencies and editable project installation remain separate so setup does not resolve a second dependency set. Tests run by default; `-SkipTests` is explicit.
OPEN: This verifies the Demo Analyzer development and CLI baseline with unit/smoke tests, not a real FACEIT `.dem` regression in this checkout. No demo fixture is committed, and the separate 2D/3D Viewer preparation workstreams remain unowned by Codex.
NEXT: Run one source-controlled or explicitly supplied representative demo regression through `iy-analyze`, validate the `iy.analysis/v1` output and disclosed optional-channel limitations, and record a reproducible non-sensitive fixture strategy before expanding Analyzer behavior.
COMMIT/PR: Benchmark source deployment helper `ab12871`; this Demo Analyzer baseline handoff is the next commit on `dev/v1-foundation`.
