# Codex

STATUS: done
TASK: Establish the technical 2D Analyzer / Viewer foundation
BRANCH: `dev/v1-foundation`
CHANGED: Added separate `iy.replay/v1` export in `improve_yourself.replay` without changing `iy.analysis/v1`. It verifies matching demo/analysis hashes, requests Awpy pitch/yaw, selects one exact first-to-last-kill scene per Multi-Kill marker, uniformly caps each scene at 256 unique tick frames, preserves CS2 world coordinates, and counts omitted incomplete snapshots instead of inventing positions. Added contract tests and expanded `docs/2D_VIEWER_FOUNDATION.md`. Downloaded official Awpy map resource patch `17595823` locally only; no radar binary was committed. No 3D, benchmark, smoke, geometry, or product UI source changed.
VERIFIED: 14/14 tests pass. Real `de_mirage` export for source SHA-256 `732855381761ad41aaea58dbdb458e8c85cb6e4bccd112f534ad3674f81f00c3` produced 6 scenes at the exact 256-frame cap, a 4,444,667-byte ignored artifact, and disclosed 512 omitted incomplete snapshots. Source hash matched. Official Awpy map patch supplies `de_mirage.png` SHA-256 `af139a92cf929214f7c0ac35e2d8c82bad385d83e71eeaf7b955dac96467a136` and transform `pos_x=-3230`, `pos_y=1713`, `scale=5`, `rotate=0` locally.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: The data foundation is complete. A visible 2D UI renderer and visual world-to-radar projection check remain a separate work object; the locally downloaded radar's upstream game-asset provenance should be reviewed before redistribution.
NEXT: Coordination can assign the 2D renderer as a concrete work object or select the remaining 3D inventory; do not commit the local radar binary without explicit provenance approval.
COMMIT/PR: 2D inventory boundary `8fb313f`; validated `iy.replay/v1` foundation is the next commit on `dev/v1-foundation`.
