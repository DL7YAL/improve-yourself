# Codex

STATUS: blocked
TASK: Build and validate the visible 2D replay renderer
BRANCH: `dev/v1-foundation`
CHANGED: Added `improve_yourself.viewer`, the `iy-replay-viewer` entry point, self-contained local HTML output, scene/frame controls, playback, team-colored positions, view directions, schema validation and safe embedded JSON. Added projection/HTML tests and renderer documentation. No radar binary, real replay artifact or generated viewer was committed. Existing `iy.analysis/v1` and `iy.replay/v1` data contracts were not changed.
VERIFIED: 18/18 tests pass. Mirage projection corners are pinned by tests for `pos_x=-3230`, `pos_y=1713`, `scale=5`. The real ignored viewer was generated from source SHA-256 `732855381761ad41aaea58dbdb458e8c85cb6e4bccd112f534ad3674f81f00c3` with local radar SHA-256 `af139a92cf929214f7c0ac35e2d8c82bad385d83e71eeaf7b955dac96467a136`. Two Kubus file-hash jobs (`job-20260816T234824125-f01287`, `job-20260816T234824152-c09461`) independently fingerprinted the canonical Experimental UI and Tactical Replay viewer sources; both matched the Brain's independent original-file hashes.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: WAITING_FOR_TRISTAN: Open the generated local `results/replay/732855381761.viewer.html` in a browser and confirm that the six real Mirage scenes place players plausibly on the radar and that their direction lines align with expected sight directions. Windows Computer Use stopped at its browser URL-policy boundary, so Codex did not claim visual validation. Radar redistribution provenance remains unresolved; the binary stays local.
NEXT: Tristan performs the bounded real Mirage sight check. After PASS/FAIL is recorded, mark this work object DONE or repair only a concrete observed renderer defect, then continue the V1 prototype inventory/integration sequence.
COMMIT/PR: Data foundation `19e1cce`; visible renderer commit pending on `dev/v1-foundation`.
