# Codex

STATUS: partial
TASK: V1 consolidation: visible 2D renderer, prototype inventory and read-only System Check baseline
BRANCH: `dev/v1-foundation`
CHANGED: Added the local `iy-replay-viewer`; completed the seven-source prototype inventory; added `iy.system_check/v1` and `iy-system-check` for non-elevated, read-only Windows/CPU/RAM/mainboard/BIOS/GPU/refresh-rate/Secure-Boot/TPM detection. No system change, optimizer apply path, legacy executable, machine-specific default, radar binary, real replay or generated result was committed.
VERIFIED: 21/21 tests pass. The real system run reports six OK and two REVIEW with zero ACTION_REQUIRED; Secure Boot and TPM remain REVIEW because their raw non-elevated Windows fields are unavailable. A detected null-to-false TPM conversion was fixed and regression-covered before commit. Mirage projection corners remain pinned for `pos_x=-3230`, `pos_y=1713`, `scale=5`. Two reviewed Kubus hash jobs matched independent original-file hashes.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: WAITING_FOR_TRISTAN: Open the generated local `results/replay/732855381761.viewer.html` in a browser and confirm that the six real Mirage scenes place players plausibly on the radar and that their direction lines align with expected sight directions. Windows Computer Use stopped at its browser URL-policy boundary, so Codex did not claim visual validation. Radar redistribution provenance remains unresolved; the binary stays local.
NEXT: Tristan performs the bounded real Mirage sight check when available. Independently, continue consolidation by defining the smallest integrated local V1 workflow over System Check, analysis and replay artifacts; Optimizer mutation/restore remains a later, separately reviewed safety boundary.
COMMIT/PR: Visible renderer `1fa6079`; prototype inventory `2d7be19`; System Check baseline is the next commit on `dev/v1-foundation`.
