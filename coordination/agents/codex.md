# Codex

STATUS: partial
TASK: V1 consolidation: reduced local review surface over the integrated workflow
BRANCH: `dev/v1-foundation`
CHANGED: Added the generated `review.html` human entry point to `iy-workflow` and its manifest. It presents summary metrics, read-only System Check cards, user-facing data-quality limitations, seven-scene list and a local Tactical Replay link. Technical parser exceptions are filtered from the reduced UI while the structured JSON remains unchanged for diagnostics. No automatic verdict, persistent review decision, Windows/Optimizer change or source schema change was added.
VERIFIED: 25/25 tests pass. Real Mirage workflow regenerated successfully. The review surface passed schema/hash validation and a real 1440x1200 Firefox headless render: responsive cards, OK/REVIEW distinction, all seven scenes, explicit human-review boundary and viewer entry are readable. A visible raw Awpy KeyError was found in QA, filtered from the user surface, regression-tested and confirmed absent while the meaningful Footstep limitation remains.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: WAITING_FOR_TRISTAN: Open the generated local `results/replay/732855381761.viewer.html` in a browser and confirm that the six real Mirage scenes place players plausibly on the radar and that their direction lines align with expected sight directions. Windows Computer Use stopped at its browser URL-policy boundary, so Codex did not claim visual validation. Radar redistribution provenance remains unresolved; the binary stays local.
NEXT: Tristan performs the bounded real Mirage sight check when available. Independently, define and implement local review-state persistence (unreviewed/reviewed/discarded/clip-worthy plus notes) as a separate user-authored artifact bound to source and scene identity; no automated conclusion. Optimizer mutation/restore remains a later safety boundary.
COMMIT/PR: Integrated workflow `c9731bb`; reduced review surface is the next commit on `dev/v1-foundation`.
