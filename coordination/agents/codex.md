# Codex

STATUS: partial
TASK: V1 consolidation: source-bound local review-state persistence
BRANCH: `dev/v1-foundation`
CHANGED: Added `iy.review_state/v1`, atomic state writes, strict source/scene/state/note validation and `iy-review-server`. The server binds only to `127.0.0.1`, enforces request size and same-origin checks, and serves the existing run directory plus a narrow state GET/POST API. The review page now loads/saves `unreviewed`, `reviewed`, `discarded` or `clip-worthy` and a 2,000-character note per known scene. Analysis and replay remain immutable.
VERIFIED: 28/28 tests pass, including real loopback GET/POST persistence and rejection of a foreign Origin. The real Mirage workflow created source-bound state for all seven scenes; the live local service returned `iy.review_state/v1` with seven `unreviewed` entries. A 1440x1500 Firefox headless render verified controls, notes and loaded-state confirmation. The service was then stopped normally; no review judgment was fabricated.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: WAITING_FOR_TRISTAN: Open the generated local `results/replay/732855381761.viewer.html` in a browser and confirm that the six real Mirage scenes place players plausibly on the radar and that their direction lines align with expected sight directions. Windows Computer Use stopped at its browser URL-policy boundary, so Codex did not claim visual validation. Radar redistribution provenance remains unresolved; the binary stays local.
NEXT: Tristan performs the bounded real Mirage sight check when available. Independently, close the remaining V1 foundation packaging/startup gap: one supported local launcher that runs the workflow, starts the loopback review service and presents the review URL without hiding logs or safety boundaries. Optimizer mutation/restore remains a later safety boundary.
COMMIT/PR: Reduced review surface `bec446d`; review persistence is the next commit on `dev/v1-foundation`.
