# Codex

STATUS: done
TASK: Version the validated Nuke benchmark authoring sources in the shared repository
BRANCH: `dev/v1-foundation`
CHANGED: Imported the authoritative project-authored addon sources without altering their contents: `assets/maps/improve_yourself_benchmark/maps/improve_yourself_benchmark.vmap` and `assets/maps/improve_yourself_benchmark/scripts/benchmark_controller.js`. Added a scoped source README with provenance, deploy topology, exclusions, and SHA-256 hashes; linked the snapshot from `assets/README.md`; changed the stale Benchmark Runtime cockpit entry from `BLOCKED` to `DONE`. No compiled VPK, cache, log, backup, Valve example/type file, post-processing default, sound event, or audio asset was imported.
VERIFIED: The staged repository map SHA-256 is `A37273C6E27AB8357068DC3FF064888EF82C2C11C84C668B85CB1FE910036572`, byte-identical to both the active local addon map and its `post-corridor-floor-20260814` backup. The staged repository controller SHA-256 is `41BA031586C168CB368875CE275B02DE7BB0B2879B2CBD0115EA47CE8778DCFA`, byte-identical to both the active controller and its `post-camera-health-20260814` backup; snapshot-local Git attributes preserve those source bytes across checkout. The source state was already runtime-validated after reboot with Full Compile `OK: 22 compiled, 0 failed, 1 skipped, 0m:22s`, total elapsed `23.262s`, followed by restored early-Nuke Graybox/world visibility after the pale flash/smoke interval. `git diff --cached --check` passes. Repository Python tests were not rerun in this fresh checkout because neither the available Python 3.13 nor 3.14 environment has `pytest` installed; the preceding Analyzer verification remains the last test evidence and this import changes no Python code.
DECISIONS: Store the self-contained project-authored benchmark source snapshot under the repository's existing `assets/maps` category while preserving its addon-relative `maps/` and `scripts/` paths. Exclude generated and third-party content. Preserve the validated camera choreography, utility timing, smoke placement, and geometry exactly.
OPEN: The installed Workshop Tools addon remains the currently deployed copy; the repository now has an integrity-verifiable source snapshot but no automated one-way deploy/sync command. The benchmark remains a graybox and any visual refinement is a separate future product/design task.
NEXT: Define a safe one-way deployment/verification helper only when another Hammer editing cycle is approved, so repository and installed-addon sources cannot drift silently.
COMMIT/PR: Source snapshot and handoff are the next commit on `dev/v1-foundation`; preceding reboot revalidation is `c4e77cf`.
