# Codex

STATUS: partial
TASK: Run and validate a representative real-demo Analyzer regression
BRANCH: `dev/v1-foundation`
CHANGED: Added `improve_yourself.validation`, a portable validator for the complete `iy.analysis/v1` result contract and cross-field invariants: schema/hash/disclaimer, kill field types, exactly one round-wide marker for every player/round with at least three kills, marker counts/tick ranges/victims matching kill data, valid quality states, disjoint available/missing channels, and the material footstep limitation. Added four synthetic validator tests. Added `tools/dev/Run-DemoRegression.ps1`, which requires one explicit local demo, runs the installed CLI into ignored `results/regression/<sha-prefix>/`, validates the result, and writes a player-name-free aggregate summary. Added `docs/DEMO_REGRESSION.md` with the non-sensitive fixture/evidence policy and linked it from project/tool documentation. No raw demo or real analysis result was copied or committed.
VERIFIED: The known prior corpus path `D:\downloads\Improve Yourself\Demos` no longer exists. No supported demo file was found under the current user Downloads, Documents/Codex, or ChatGPT project mirror. The expanded test suite passes `11/11`, including valid round-wide Multi-Kill/footstep limitation, channel-overlap rejection, marker/kill mismatch rejection, and malformed marker identity without validator crash. Both development PowerShell scripts pass AST parsing. The regression harness rejects a missing demo before output with exit code `1`. `.gitignore` demonstrably excludes `results/regression/...`. `git diff --check` passes.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: The real parser-to-result regression cannot run because no `.dem`, `.dem.zst`, or `.dem.bz2` is currently available in the known local locations. Therefore no current map, tickrate, aggregate kill count, optional-channel set, or real `iy.analysis/v1` PASS is claimed.
NEXT: Provide or restore one representative local demo and run `.\tools\dev\Run-DemoRegression.ps1 -Demo '<path>'`; then record only its SHA-256 and aggregate validated evidence in this handoff.
COMMIT/PR: Reproducible development baseline `0af4e0e`; this regression-harness/blocked-evidence handoff is the next commit on `dev/v1-foundation`.
