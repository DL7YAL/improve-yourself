# Codex

STATUS: partial
TASK: V1 consolidation: integrated local System Check, analysis, replay and viewer workflow
BRANCH: `dev/v1-foundation`
CHANGED: Added `iy-workflow` and `iy.workflow/v1`, connecting the read-only System Check, `iy.analysis/v1`, bounded `iy.replay/v1` and the self-contained viewer in one local SHA-keyed run directory. The manifest contains relative artifact paths, review policy and no original demo path. No Windows/Optimizer change, elevation, upload, automatic verdict, real artifact or radar binary was committed.
VERIFIED: 23/23 tests pass. Real local Mirage source SHA-256 `b7532c82381b57cb87a983ecb1286e7c565e9b69b083750e9814c650a24730db` completed end to end: system 6 OK/2 REVIEW/0 ACTION_REQUIRED, valid `iy.analysis/v1` with 122 kills and 7 Multi-Kill markers, 7 replay scenes with 1,792 frames and 0 omitted incomplete snapshots, embedded local-radar viewer, and matching source hash across manifest/analysis/replay.
DECISIONS: Real demos and detailed results remain local and uncommitted. Regression evidence records source SHA-256 and aggregate map/tickrate/count/channel/quality facts, not player names or kill details. Missing `footsteps`/`player_sound` is a disclosed source-capability limitation, not a damaged-demo verdict. Do not commit a real binary fixture unless it is deliberately generated or licensed, non-sensitive, small, and provenance-documented.
OPEN: WAITING_FOR_TRISTAN: Open the generated local `results/replay/732855381761.viewer.html` in a browser and confirm that the six real Mirage scenes place players plausibly on the radar and that their direction lines align with expected sight directions. Windows Computer Use stopped at its browser URL-policy boundary, so Codex did not claim visual validation. Radar redistribution provenance remains unresolved; the binary stays local.
NEXT: Tristan performs the bounded real Mirage sight check when available. Independently, build the reduced review surface over `iy.workflow/v1` so a user can inspect system findings and open the generated tactical scenes without working directly with JSON. Optimizer mutation/restore remains a later, separately reviewed safety boundary.
COMMIT/PR: Visible renderer `1fa6079`; prototype inventory `2d7be19`; System Check `14bb7a2`; integrated workflow is the next commit on `dev/v1-foundation`.
