# Codex

STATUS: done
TASK: Add safe one-way verification and deployment for the versioned CS2 benchmark sources
BRANCH: `dev/v1-foundation`
CHANGED: Added `assets/maps/improve_yourself_benchmark/source-manifest.json` as the machine-readable two-file source contract. Added `tools/benchmark/Sync-BenchmarkAddon.ps1` and its README; linked the helper from the source and tools documentation. The helper resolves the default addon from Steam, verifies repository hashes before trusting them, and defaults to a read-only installed-source comparison. Explicit `-Deploy` is one-way from repository to addon, refuses an unexpected addon name, reparse-point root, target outside `content/csgo_addons`, unsafe manifest path, or unexpected manifest cardinality, and changes only missing/different manifest files. Existing different targets are hash-verified after backup under `backups/deploy-<timestamp>/`; deployed files are hash-verified again. Updated the completed cockpit row with this workflow. No benchmark source or installed addon file changed.
VERIFIED: PowerShell AST parsing reports no syntax errors. The default real-addon verification passes with both installed target hashes equal to the manifest. Running `-Deploy` against the already-matching real addon reports a no-op and writes nothing. An explicit invalid target `C:\Windows` is rejected before file operations with exit code `1`. `git diff --check` passes. The manifest retains map SHA-256 `A37273C6E27AB8357068DC3FF064888EF82C2C11C84C668B85CB1FE910036572` and controller SHA-256 `41BA031586C168CB368875CE275B02DE7BB0B2879B2CBD0115EA47CE8778DCFA`.
DECISIONS: Verification must be the default operation; deployment requires explicit `-Deploy`. Deploy only manifest-listed project sources, back up only files that will be replaced, verify the backup before overwrite, and verify every target after copy. Do not automate Hammer startup, Full Compile, or runtime validation in this helper.
OPEN: The helper has not performed a real drift replacement because the installed addon already matches; its real-addon deployment path was therefore exercised only as a verified no-op. The benchmark remains a graybox and any visual refinement is a separate future product/design task.
NEXT: Select the next V1 workstream; the Benchmark Runtime / early Nuke camera work object is complete and no further Hammer change is currently authorized.
COMMIT/PR: Source snapshot `720666b`; this deployment-helper handoff is the next commit on `dev/v1-foundation`.
