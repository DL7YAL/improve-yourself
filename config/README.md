# Configuration

Version-controlled default configuration and schemas belong here.

Machine-specific settings, secrets and personal local configuration do not belong in Git and are excluded through `.gitignore`.
